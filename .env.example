# Root environment file -- consumed by docker-compose.yml for Postgres credentials.
# Copy to .env before running: docker compose up -d
#
# Backend environment variables live in backend/.env.example -- copy that too.

POSTGRES_USER=banyan
POSTGRES_PASSWORD=changeme
POSTGRES_DB=banyanboard
