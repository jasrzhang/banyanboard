// Request/response DTOs — populated in FEAT-003 when REST endpoints are implemented.
// Domain-shape types live in domain.ts; this file is for wire-format shapes that
// differ from internal representation (e.g., paginated lists, create/update payloads).
export type {};
